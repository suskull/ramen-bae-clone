-- This migration file should contain small, incremental changes
-- Large schema recreations should not be in migration files

-- Example of what SHOULD be in a migration file:
-- ALTER TABLE categories ADD COLUMN IF NOT EXISTS description TEXT;

-- For now, this migration does nothing (safe)
SELECT 1;